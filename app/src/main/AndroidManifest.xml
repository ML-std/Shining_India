<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shiningindia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShiningIndia">
        <activity android:name=".Order.OnlinePaymentActivity" />
        <activity android:name=".Home.HomeActivity" />
        <activity android:name=".Order.CartActivity" />
        <activity android:name=".Booking.BookingActivity" />
        <activity android:name=".Menu.ItemActivity" />
        <activity android:name=".Order.OrderActivity" />
        <activity android:name=".Menu.MenuActivity" />
        <activity android:name=".Register.RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>